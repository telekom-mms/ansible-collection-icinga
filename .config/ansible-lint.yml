---
# .ansible-lint
exclude_paths:
  - .cache/ # implicit unless exclude_paths is defined in config
  - .yamllint
  - molecule/
  - .github/

warn_list:
  - fqcn[keyword]
  - var-naming[no-role-prefix]
